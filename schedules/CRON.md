# Cron Jobs

Define scheduled tasks below. APEX can edit this file to add/remove jobs.
Format: heading = job name, `cron:` expression, `prompt:` what to do, `notify:` where to send results.

<!-- No jobs configured yet. APEX can add jobs using the write_file tool. -->
